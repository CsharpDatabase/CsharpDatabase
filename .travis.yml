#
# CsharpDatabase Hook
#

# Travis language: c(sharp)
language: c

# Mono
install:
 - sudo apt-get install mono-devel mono-xbuild

script:
 - ./build.sh release

# Only watch the master branch
branches:
  only:
    - master

# Notify if needed
notifications:
  irc: "irc.rizon.net#schumix"
